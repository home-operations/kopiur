[package]
name = "xtask"
description = "Code generation tasks for kopiur: CRD YAML, RBAC manifests"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[dependencies]
kopiur-api = { workspace = true }
# `gen-ui-types` exports the SPA's TypeScript from these wire types. The crate is
# Kubernetes-free (serde + ts-rs only), so it adds no controller deps to codegen.
kopiur-ui-model = { workspace = true }
kube = { workspace = true, features = ["derive"] }
k8s-openapi = { workspace = true, features = ["v1_33"] }
serde = { workspace = true }
serde_json = { workspace = true }
serde_yaml = { workspace = true }
anyhow = { workspace = true }

[dev-dependencies]
insta = { workspace = true }

[features]
# Gates the cluster-dependent integration test. Off by default; the test is also
# `#[ignore]` so `cargo test` stays hermetic.
integration = []
