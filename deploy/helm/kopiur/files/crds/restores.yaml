# GENERATED by `cargo xtask gen-crds` — DO NOT EDIT
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: restores.kopiur.home-operations.com
spec:
  group: kopiur.home-operations.com
  names:
    categories:
    - kopiur
    kind: Restore
    plural: restores
    shortNames:
    - kopiarestore
    singular: restore
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.sourceKind
      name: Source
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for RestoreSpec via `CustomResource`
        properties:
          spec:
            description: |-
              A restore operation from a snapshot/identity into a PVC, or a passive populator source.
              Desired state of a Restore: where to read from, where to write to, and how to behave when the snapshot is missing.
            properties:
              credentialProjection:
                description: Opt-in copying of the repository's credential Secret(s) into the mover's namespace (default off).
                nullable: true
                properties:
                  enabled:
                    default: false
                    description: Copy the repository's credential Secret(s) into the namespace of each mover Job; off by default.
                    type: boolean
                type: object
              failurePolicy:
                description: Mover `Job` retry/deadline limits (`backoffLimit`, `activeDeadlineSeconds`).
                nullable: true
                properties:
                  activeDeadlineSeconds:
                    description: Mover `Job.spec.activeDeadlineSeconds` — wall-clock cap after which a running run is killed.
                    format: int64
                    nullable: true
                    type: integer
                  backoffLimit:
                    description: Mover `Job.spec.backoffLimit` — retries before a failed run is marked failed.
                    format: int32
                    nullable: true
                    type: integer
                  podStartupDeadlineSeconds:
                    description: Seconds a non-starting (wedged) mover pod may sit before the run is failed; default 300s.
                    format: int64
                    nullable: true
                    type: integer
                type: object
              mover:
                description: Per-run mover overrides for this restore's Job (resources, cache, `securityContext`).
                nullable: true
                properties:
                  cache:
                    description: Override the repository's [`CacheDefaults`] for this recipe's movers.
                    nullable: true
                    properties:
                      capacity:
                        description: Size of the PVC backing the mover's kopia cache (e.g. `10Gi`).
                        nullable: true
                        type: string
                      contentCacheSizeMb:
                        description: kopia content cache budget in MiB (`--content-cache-size-mb`).
                        format: int64
                        nullable: true
                        type: integer
                      metadataCacheSizeMb:
                        description: kopia metadata cache budget in MiB (`--metadata-cache-size-mb`).
                        format: int64
                        nullable: true
                        type: integer
                      mode:
                        description: How a mover's kopia cache volume is provisioned.
                        enum:
                        - Ephemeral
                        - Persistent
                        nullable: true
                        type: string
                      storageClassName:
                        description: StorageClass for the cache PVC; absent uses the cluster default.
                        nullable: true
                        type: string
                    type: object
                  inheritSecurityContextFrom:
                    description: Copy the UID/GID security context from a live workload instead of setting it explicitly.
                    nullable: true
                    oneOf:
                    - required:
                      - workloadSelector
                    - required:
                      - pvcConsumer
                    properties:
                      pvcConsumer:
                        description: 'Backup sources only: auto-derive the workload from the PVC this snapshot backs up.'
                        properties:
                          container:
                            description: Which container within the matched consumer pod to inherit from; absent uses the first/only.
                            nullable: true
                            type: string
                        type: object
                      workloadSelector:
                        description: Inherit from workload pod(s) matched by an explicit label selector (backup or restore).
                        properties:
                          container:
                            description: Which container within the matched pod; absent uses the first/only container.
                            nullable: true
                            type: string
                          podSelector:
                            description: Label selector matching the workload pod(s) to read context/hooks from.
                            properties:
                              matchExpressions:
                                description: matchExpressions is a list of label selector requirements. The requirements are ANDed.
                                items:
                                  description: A label selector requirement is a selector that contains values, a key, and an operator that relates the key and values.
                                  properties:
                                    key:
                                      description: key is the label key that the selector applies to.
                                      type: string
                                    operator:
                                      description: operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
                                      type: string
                                    values:
                                      description: values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
                                      items:
                                        type: string
                                      type: array
                                  required:
                                  - key
                                  - operator
                                  type: object
                                type: array
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
                                type: object
                            type: object
                        required:
                        - podSelector
                        type: object
                    type: object
                  podSecurityContext:
                    description: Pod security context for the mover (notably `fsGroup` for group-writable restore volumes).
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  privilegedMode:
                    description: Opt-in, namespace-gated privileged mode; preserves UID/GID on restore.
                    nullable: true
                    type: boolean
                  resources:
                    description: Resource requests/limits for the mover container.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  securityContext:
                    description: Container security context for the mover; merged field-wise over the defaults and hardened base.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  ttlSecondsAfterFinished:
                    description: Per-recipe override of `Job.spec.ttlSecondsAfterFinished` so finished Jobs self-GC.
                    format: int64
                    nullable: true
                    type: integer
                type: object
              options:
                description: kopia restore behavior (file deletion, permission/atomicity handling).
                nullable: true
                properties:
                  enableFileDeletion:
                    description: Delete files in the target that are not present in the snapshot (exact mirror); off by default.
                    type: boolean
                  ignorePermissionErrors:
                    description: Continue past permission errors during restore (default true).
                    nullable: true
                    type: boolean
                  writeFilesAtomically:
                    description: Write files atomically via a temp file + rename (default true).
                    nullable: true
                    type: boolean
                type: object
              policy:
                description: What to do when the referenced snapshot doesn't exist yet, and how long to wait.
                nullable: true
                properties:
                  onMissingSnapshot:
                    description: |-
                      What to do when the resolved source matches no snapshot. Closed enum. ADR §4.6 (G7).

                      ```
                      use kopiur_api::restore::OnMissingSnapshot;

                      // Fail-closed is the default so an explicit restore can't silently no-op.
                      assert_eq!(OnMissingSnapshot::default(), OnMissingSnapshot::Fail);
                      assert_eq!(serde_json::to_value(OnMissingSnapshot::Continue).unwrap(), "Continue");
                      ```
                    enum:
                    - Fail
                    - Continue
                    nullable: true
                    type: string
                  waitTimeout:
                    description: How long to wait for the source snapshot to appear before giving up (e.g. `5m`).
                    nullable: true
                    type: string
                type: object
              repository:
                description: The repository to read from; derived from `source` when omitted, required only with `source.identity`.
                nullable: true
                properties:
                  kind:
                    default: Repository
                    description: Which repository CRD this points at; defaults to [`RepositoryKind::Repository`].
                    enum:
                    - Repository
                    - ClusterRepository
                    type: string
                  name:
                    description: Name of the referenced `Repository`/`ClusterRepository`.
                    type: string
                  namespace:
                    description: Cross-namespace `Repository` reference; ignored/forbidden for `ClusterRepository`.
                    nullable: true
                    type: string
                required:
                - name
                type: object
              source:
                description: Where to read data from (snapshotRef, fromPolicy, or identity).
                oneOf:
                - required:
                  - snapshotRef
                - required:
                  - fromPolicy
                - required:
                  - identity
                properties:
                  fromPolicy:
                    description: A `SnapshotPolicy` CR, resolved via identity even with no `Snapshot` CR present (deploy-or-restore).
                    properties:
                      asOf:
                        description: Restore the newest snapshot at or before this RFC3339 timestamp (point-in-time).
                        nullable: true
                        type: string
                      name:
                        description: Name of the `SnapshotPolicy` whose identity selects the snapshot.
                        type: string
                      namespace:
                        description: Namespace of the `SnapshotPolicy`; absent = the `Restore`'s own namespace.
                        nullable: true
                        type: string
                      offset:
                        default: 0
                        description: 'Which snapshot to pick: 0 = latest, 1 = previous, and so on.'
                        format: int64
                        type: integer
                    required:
                    - name
                    type: object
                  identity:
                    description: A raw kopia identity (foreign writers / aged-out catalog); requires `spec.repository`.
                    properties:
                      asOf:
                        description: Restore the newest snapshot at or before this RFC3339 timestamp (point-in-time).
                        nullable: true
                        type: string
                      hostname:
                        description: The kopia `hostname` to match.
                        type: string
                      offset:
                        description: 'Which snapshot to pick: 0 = latest, 1 = previous, and so on.'
                        format: int64
                        nullable: true
                        type: integer
                      snapshotID:
                        description: Pin an exact kopia snapshot by ID.
                        nullable: true
                        type: string
                      sourcePath:
                        description: The kopia source path to match; absent matches any path for the identity.
                        nullable: true
                        type: string
                      username:
                        description: The kopia `username` to match.
                        type: string
                    required:
                    - hostname
                    - username
                    type: object
                  snapshotRef:
                    description: A `Snapshot` CR (scheduled, manual, or discovered).
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object; absent = same namespace as the referrer.
                        nullable: true
                        type: string
                    required:
                    - name
                    type: object
                type: object
              target:
                description: Where to write the restored data (pvc, pvcRef, or populator).
                oneOf:
                - required:
                  - pvc
                - required:
                  - pvcRef
                - required:
                  - populator
                properties:
                  populator:
                    description: 'Passive populator mode: the restore is claimed by a PVC''s `spec.dataSourceRef`.'
                    type: object
                  pvc:
                    description: Operator creates the PVC.
                    properties:
                      accessModes:
                        description: Access modes for the new PVC (e.g. `["ReadWriteOnce"]`).
                        items:
                          type: string
                        type: array
                      capacity:
                        description: Requested size of the new PVC (e.g. `100Gi`).
                        nullable: true
                        type: string
                      name:
                        description: Name of the PVC to create.
                        type: string
                      storageClassName:
                        description: StorageClass for the new PVC; absent uses the cluster default.
                        nullable: true
                        type: string
                    required:
                    - name
                    type: object
                  pvcRef:
                    description: Write into an existing PVC.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object; absent = same namespace as the referrer.
                        nullable: true
                        type: string
                    required:
                    - name
                    type: object
                type: object
            required:
            - source
            - target
            type: object
            x-kubernetes-validations:
            - message: exactly one of target.pvc, target.pvcRef, target.populator
              rule: '[has(self.target.pvc), has(self.target.pvcRef), has(self.target.populator)].filter(x, x).size() == 1'
          status:
            description: Observed state of a Restore, written by the controller/mover.
            nullable: true
            properties:
              conditions:
                description: Standard Kubernetes conditions carrying the human-readable status/reason.
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating details about the transition. This may be an empty string.
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failure:
                description: Structured terminal-failure detail (kopia error class, stderr tail, retry hint).
                nullable: true
                properties:
                  exitCode:
                    description: The process exit code, if one was reported.
                    format: int32
                    nullable: true
                    type: integer
                  kopiaErrorClass:
                    description: kopia error class (e.g. `RepositoryUnavailable`, `AuthFailure`).
                    type: string
                  message:
                    description: 'A short human-readable message: what failed, why, and how to fix it.'
                    type: string
                  retryRecommended:
                    description: Whether retrying the same operation unchanged could succeed.
                    type: boolean
                  stderrTail:
                    description: |-
                      The last lines of kopia's stderr, if any were captured (bounded by
                      [`MAX_LOG_TAIL_BYTES`]).
                    nullable: true
                    type: string
                required:
                - kopiaErrorClass
                - message
                - retryRecommended
                type: object
              logTail:
                description: The last lines of the run's output, written by the mover at the terminal transition.
                nullable: true
                type: string
              observedGeneration:
                description: '`metadata.generation` last reconciled, so stale status is detectable.'
                format: int64
                nullable: true
                type: integer
              phase:
                description: Lifecycle phase of a restore.
                enum:
                - Pending
                - Resolving
                - Restoring
                - Completed
                - Failed
                nullable: true
                type: string
              progress:
                description: Bytes/files restored so far, patched periodically by the mover.
                nullable: true
                properties:
                  bytesRestored:
                    description: Total bytes restored so far.
                    format: int64
                    nullable: true
                    type: integer
                  filesRestored:
                    description: Total files restored so far.
                    format: int64
                    nullable: true
                    type: integer
                type: object
              resolved:
                description: The source resolved and pinned at admission; never re-resolved.
                nullable: true
                properties:
                  identity:
                    description: The resolved kopia identity (`username@hostname:path`) of the snapshot.
                    nullable: true
                    properties:
                      hostname:
                        description: The final `hostname` kopia records, fixed at admission.
                        type: string
                      sourcePath:
                        description: The resolved snapshot source path, when applicable (`username@hostname:path`).
                        nullable: true
                        type: string
                      username:
                        description: The final `username` kopia records, fixed at admission.
                        type: string
                    required:
                    - hostname
                    - username
                    type: object
                  kopiaSnapshotID:
                    description: The exact kopia snapshot manifest id the source resolved to; pinned once.
                    nullable: true
                    type: string
                  pinnedAt:
                    description: Timestamp at which the source was pinned (RFC3339).
                    nullable: true
                    type: string
                  repository:
                    description: The repository the snapshot lives in, resolved from the source.
                    nullable: true
                    properties:
                      kind:
                        default: Repository
                        description: Which repository CRD this points at; defaults to [`RepositoryKind::Repository`].
                        enum:
                        - Repository
                        - ClusterRepository
                        type: string
                      name:
                        description: Name of the referenced `Repository`/`ClusterRepository`.
                        type: string
                      namespace:
                        description: Cross-namespace `Repository` reference; ignored/forbidden for `ClusterRepository`.
                        nullable: true
                        type: string
                    required:
                    - name
                    type: object
                  resolution:
                    description: Which outcome the source resolution pinned, once and never re-resolved.
                    enum:
                    - Snapshot
                    - NoSnapshot
                    nullable: true
                    type: string
                  snapshotRef:
                    description: The concrete `Snapshot` CR the source resolved to, when applicable.
                    nullable: true
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object; absent = same namespace as the referrer.
                        nullable: true
                        type: string
                    required:
                    - name
                    type: object
                type: object
              sourceKind:
                description: The pinned source kind (`SnapshotRef`/`FromPolicy`/`Identity`); backs the `SOURCE` printer column.
                nullable: true
                type: string
              target:
                description: Resolved target details (the PVC written to / populator handshake).
                nullable: true
                properties:
                  pvcPrime:
                    description: Populator handshake (passive / pvc-create modes).
                    nullable: true
                    type: string
                  pvcRef:
                    description: The PVC actually written to (created or pre-existing).
                    nullable: true
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object; absent = same namespace as the referrer.
                        nullable: true
                        type: string
                    required:
                    - name
                    type: object
                type: object
              timing:
                description: Start/end timestamps for the restore run.
                nullable: true
                properties:
                  endTime:
                    description: When the restore reached a terminal phase (RFC3339).
                    nullable: true
                    type: string
                  startTime:
                    description: When the mover began restoring (RFC3339).
                    nullable: true
                    type: string
                type: object
            type: object
        required:
        - spec
        title: Restore
        type: object
    served: true
    storage: true
    subresources:
      status: {}
